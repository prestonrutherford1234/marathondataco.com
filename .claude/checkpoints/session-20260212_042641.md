# Session Checkpoints — 2026-02-12

### Checkpoint 1 (25% — Quarter Session) (04:12)

[PATTERN] The bot can count tags within an HTML file using `content.count('<tag>')` and `content.count('</tag>')` in a python script.
[PATTERN] The bot can rename multiple directories at once using `mv dir1 new_dir1 && mv dir2 new_dir2 ...`
[CONTEXT] The current desired site is in `marathon-site` (now `marathon-website-brand-measurement`).
[CONTEXT] Other site directories are `marathon-current-website-redesign` (now `marathon-website-growth-os`) and `marathon-website-redesign` (now `marathon-website-lead-gen`).
